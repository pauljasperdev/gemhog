---
phase: 04.1-resend-email-provider
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/email.ts
  - infra/secrets.ts
  - infra/api.ts
  - infra/web.ts
  - sst.config.ts
autonomous: true

must_haves:
  truths:
    - "SES infrastructure is completely removed from SST config"
    - "RESEND_API_KEY is injected as env var to both Lambda and Next.js"
    - "ResendApiKey SST secret exists for production deployment"
    - "CloudflareZoneId secret is preserved (still used by router and web)"
  artifacts:
    - path: "infra/secrets.ts"
      provides: "ResendApiKey SST secret"
      contains: "ResendApiKey"
    - path: "infra/api.ts"
      provides: "RESEND_API_KEY env var injection for Lambda"
      contains: "RESEND_API_KEY"
    - path: "infra/web.ts"
      provides: "RESEND_API_KEY env var injection for Next.js"
      contains: "RESEND_API_KEY"
    - path: "sst.config.ts"
      provides: "Clean config without email.ts import"
  key_links:
    - from: "infra/api.ts"
      to: "infra/secrets.ts"
      via: "secrets.ResendApiKey.value"
      pattern: "secrets\\.ResendApiKey\\.value"
    - from: "infra/web.ts"
      to: "infra/secrets.ts"
      via: "secrets.ResendApiKey.value"
      pattern: "secrets\\.ResendApiKey\\.value"
---

<objective>
Remove AWS SES infrastructure and add Resend API key to SST deployment configuration.

Purpose: Complete the SES-to-Resend migration at the infrastructure layer. SES DNS records and the Email component are removed. The Resend API key is injected as an environment variable to both the Hono Lambda and Next.js app via SST secrets.

Output: Clean SST config with no SES references, ResendApiKey secret, RESEND_API_KEY env var in both api.ts and web.ts.
</objective>

<execution_context>
@/Users/bot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-resend-email-provider/04.1-RESEARCH.md
@infra/email.ts
@infra/secrets.ts
@infra/api.ts
@infra/web.ts
@sst.config.ts
@infra/router.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ResendApiKey secret and update env injection in api.ts and web.ts</name>
  <files>
    infra/secrets.ts
    infra/api.ts
    infra/web.ts
  </files>
  <action>
    1. Update `infra/secrets.ts`:
       - Add `ResendApiKey: new sst.Secret("ResendApiKey"),` after the PosthogKey entry
       - Add a comment `// Email sending (Resend)` before it for consistency with existing grouping
       - Keep `CloudflareZoneId` -- it is still used by `infra/router.ts` and `infra/web.ts`

    2. Update `infra/api.ts`:
       - Replace the `SES_FROM_EMAIL: "hello@gemhog.com",` line with `RESEND_API_KEY: secrets.ResendApiKey.value,`
       - Update the comment from `// Email infrastructure` to `// Email sending (Resend)`

    3. Update `infra/web.ts`:
       - Replace the `SES_FROM_EMAIL: "hello@gemhog.com",` line with `RESEND_API_KEY: secrets.ResendApiKey.value,`
       - Update the comment from `// Email infrastructure` to `// Email sending (Resend)`
  </action>
  <verify>
    - `grep "ResendApiKey" infra/secrets.ts` shows the secret declaration
    - `grep "RESEND_API_KEY" infra/api.ts infra/web.ts` shows both injection points
    - `grep "SES_FROM_EMAIL" infra/` returns nothing
    - `grep "CloudflareZoneId" infra/secrets.ts` confirms it's still present
  </verify>
  <done>
    - ResendApiKey SST secret exists in secrets.ts
    - Both api.ts and web.ts inject RESEND_API_KEY from the secret
    - SES_FROM_EMAIL is removed from all infra files
    - CloudflareZoneId preserved for router.ts and web.ts DNS
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete SES infrastructure and clean up sst.config.ts</name>
  <files>
    infra/email.ts
    sst.config.ts
  </files>
  <action>
    1. Delete `infra/email.ts` entirely:
       ```bash
       rm infra/email.ts
       ```
       This removes the `sst.aws.Email` component that provisioned SES with DKIM/SPF/DMARC DNS records via Cloudflare. Resend manages its own DNS records through the Resend dashboard (already configured per context decisions).

    2. Update `sst.config.ts`:
       - Remove the line `await import("./infra/email");` from the `run()` function
       - Keep all other imports unchanged (secrets, neon, api, web)
  </action>
  <verify>
    - `ls infra/email.ts` returns "No such file or directory"
    - `grep "email" sst.config.ts` returns nothing
    - `grep "email" infra/*.ts` returns nothing (no SES references remain anywhere in infra)
    - The SST config file is valid TypeScript (no dangling imports)
  </verify>
  <done>
    - `infra/email.ts` is deleted
    - `sst.config.ts` no longer imports email infrastructure
    - No SES references remain anywhere in the `infra/` directory
    - SST config is clean and deployable
  </done>
</task>

</tasks>

<verification>
Verify no SES references remain in the infrastructure:

```bash
grep -r "SES" infra/ sst.config.ts
grep -r "email.ts" infra/ sst.config.ts
grep -r "SES_FROM_EMAIL" infra/
```

All three commands should return no matches.

Verify Resend configuration is complete:
```bash
grep "ResendApiKey" infra/secrets.ts infra/api.ts infra/web.ts
```

Should show 3 matches (secret declaration + two injection points).
</verification>

<success_criteria>
1. `infra/email.ts` deleted (SES Email component removed)
2. `sst.config.ts` no longer imports `./infra/email`
3. `infra/secrets.ts` has `ResendApiKey` SST secret
4. `infra/api.ts` injects `RESEND_API_KEY: secrets.ResendApiKey.value`
5. `infra/web.ts` injects `RESEND_API_KEY: secrets.ResendApiKey.value`
6. `CloudflareZoneId` preserved (used by router.ts, web.ts)
7. No references to SES, SES_FROM_EMAIL, or email.ts remain in infra
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-resend-email-provider/04.1-02-SUMMARY.md`
</output>
