---
phase: 04.1-resend-email-provider
plan: 02
subsystem: infrastructure
tags: [sst, resend, ses, email, infra]
dependency_graph:
  requires: []
  provides: ["ResendApiKey SST secret", "RESEND_API_KEY env injection for Lambda and Next.js", "Clean SST config without SES"]
  affects: ["04.1-01 (parallel application code changes)"]
tech_stack:
  added: []
  removed: ["sst.aws.Email (SES component)"]
  patterns: ["SST secret-based env var injection for third-party API keys"]
key_files:
  created: []
  modified: ["infra/secrets.ts", "infra/api.ts", "infra/web.ts", "sst.config.ts"]
  deleted: ["infra/email.ts"]
decisions:
  - id: "04.1-02-D1"
    decision: "ResendApiKey grouped with comment before PostHog section in secrets.ts"
    rationale: "Maintains consistent grouping pattern (comment + secret entries)"
metrics:
  duration: "1min 20s"
  completed: "2026-01-30"
  tasks: "2/2"
---

# Phase 4.1 Plan 02: SST Infrastructure Migration (SES to Resend) Summary

**One-liner:** Removed SES Email component from SST config and injected ResendApiKey secret as RESEND_API_KEY env var to both Lambda and Next.js.

## What Was Done

### Task 1: Add ResendApiKey secret and update env injection
**Commit:** `92fde80` feat(04.1-02): add ResendApiKey secret and update env injection

- Added `ResendApiKey` SST secret to `infra/secrets.ts` with grouping comment
- Replaced `SES_FROM_EMAIL: "hello@gemhog.com"` with `RESEND_API_KEY: secrets.ResendApiKey.value` in `infra/api.ts`
- Replaced `SES_FROM_EMAIL: "hello@gemhog.com"` with `RESEND_API_KEY: secrets.ResendApiKey.value` in `infra/web.ts`
- Updated comments from "Email infrastructure" to "Email sending (Resend)" in both files

### Task 2: Delete SES infrastructure and clean up sst.config.ts
**Commit:** `7f52991` chore(04.1-02): delete SES infrastructure and clean sst.config.ts

- Deleted `infra/email.ts` (SES Email component with DKIM/SPF/DMARC DNS records via Cloudflare)
- Removed `await import("./infra/email");` from `sst.config.ts` run() function
- Resend manages its own DNS records through the Resend dashboard (no SST provisioning needed)

## Verification Results

| Check | Result |
|-------|--------|
| `grep -r "SES" infra/ sst.config.ts` | No matches (clean) |
| `grep -r "email.ts" infra/ sst.config.ts` | No matches (clean) |
| `grep -r "SES_FROM_EMAIL" infra/` | No matches (clean) |
| `grep "ResendApiKey" infra/secrets.ts infra/api.ts infra/web.ts` | 3 matches (secret + 2 injections) |
| `grep "CloudflareZoneId" infra/secrets.ts` | Present (preserved for router.ts/web.ts) |
| `ls infra/email.ts` | No such file (deleted) |
| Lefthook commit hooks (biome + typecheck) | Passed on both commits |

## Deviations from Plan

None - plan executed exactly as written.

## Decisions Made

1. **ResendApiKey placement in secrets.ts** - Placed after CloudflareZoneId and before PosthogKey with "Email sending (Resend)" comment, maintaining the existing grouping pattern.

## Success Criteria Checklist

- [x] `infra/email.ts` deleted (SES Email component removed)
- [x] `sst.config.ts` no longer imports `./infra/email`
- [x] `infra/secrets.ts` has `ResendApiKey` SST secret
- [x] `infra/api.ts` injects `RESEND_API_KEY: secrets.ResendApiKey.value`
- [x] `infra/web.ts` injects `RESEND_API_KEY: secrets.ResendApiKey.value`
- [x] `CloudflareZoneId` preserved (used by router.ts, web.ts)
- [x] No references to SES, SES_FROM_EMAIL, or email.ts remain in infra

## Next Steps

- Plan 04.1-01 (parallel) handles application code: packages/core email service, packages/env schema, email-layers.ts, subscriber router
- After both plans complete: set `ResendApiKey` secret value via `sst secret set ResendApiKey <key>` for each stage
