{
  "name": "gemhog",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "sso": "aws sso login --sso-session=gemhog",
    "dev:init": "rm -rf node_modules && pnpm install &&  pnpm playwright:install",
    "playwright:install": "pnpm exec playwright install chromium --with-deps",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:native": "pnpm --filter native dev",
    "dev:web": "pnpm --filter web dev",
    "dev:server": "pnpm --filter server dev",
    "db:push": "pnpm --filter @gemhog/core db:push",
    "db:studio": "pnpm --filter @gemhog/core db:studio",
    "db:generate": "pnpm --filter @gemhog/core db:generate",
    "db:migrate": "pnpm --filter @gemhog/core db:migrate",
    "sst:db:push": "psst shell -- pnpm sst shell --filter @gemhog/core db:push",
    "sst:db:studio": "psst shell -- pnpm --filter @gemhog/core db:studio",
    "sst:db:generate": "sst shell -- pnpm --filter @gemhog/core db:generate",
    "sst:db:migrate": "sst shell -- pnpm --filter @gemhog/core db:migrate",
    "db:start": "docker compose -f ./infra/docker-compose.yml up -d",
    "db:watch": "docker compose -f ./infra/docker-compose.yml up",
    "db:stop": "docker compose -f ./infra/docker-compose.yml stop",
    "db:down": "docker compose -f ./infra/docker-compose.yml down",
    "check": "biome check --error-on-warnings .",
    "check:fix": "biome check --write .",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:integration:up": "pnpm db:start",
    "test:e2e": "playwright test",
    "format:md": "prettier --write \"**/*.md\"",
    "security:audit": "pnpm audit --audit-level low",
    "prepare": "lefthook install",
    "verify:commit": "pnpm check && pnpm check-types && pnpm test:unit",
    "verify": "./scripts/verify.sh"
  },
  "dependencies": {
    "@gemhog/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@gemhog/config": "workspace:*",
    "@playwright/mcp": "0.0.47",
    "@playwright/test": "^1.57.0",
    "@types/node": "^22.13.14",
    "@vitest/coverage-v8": "^4.0.17",
    "happy-dom": "^20.3.3",
    "lefthook": "^2.0.15",
    "prettier": "^3.8.0",
    "sst": "^3.17.37",
    "typescript": "catalog:",
    "vitest": "^4.0.17"
  },
  "packageManager": "pnpm@10.15.1",
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.25.0",
      "lodash-es": ">=4.17.23"
    },
    "auditConfig": {}
  }
}
